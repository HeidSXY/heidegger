[package]
name = "heidegger-core"
version = "0.1.0"
edition = "2021"
description = "Core safety logic for Heidegger - deterministic safety layer for VLA robots"

[lib]
name = "heidegger_core"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
